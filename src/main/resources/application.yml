spring:
  profiles:
    active: dev

  servlet:
    multipart:
      max-file-size: 5MB
  jpa:
    open-in-view: true

server:
  forward-headers-strategy: native
  servlet:
    session:
      persistent: false
      cookie:
        secure: true
        http-only: true
        same-site: lax